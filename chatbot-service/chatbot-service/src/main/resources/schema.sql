CREATE TABLE IF NOT EXISTS placeholder (
    id INT PRIMARY KEY,
    name VARCHAR(255),
    icon VARCHAR(20)
);

CREATE TABLE IF NOT EXISTS option (
    id INT PRIMARY KEY,
    name VARCHAR(255),
    icon VARCHAR(20)
);

